#include "main.h"
/**
 * _abs - check if the char is a lowercase
 * @c: character to be passed from main
 * Return: if it is lowercase return 1 , else return 0
 */
int _abs(int c)
{
	if (c > 0)
		return (c);
	else
		return (-1 * c);
}
