#include "main.h"
/**
 * add - a function that add two integers
 * @a: first integer to be passed from main
 * @b: scond integer to be passed from main
 * Return: the sum of the two integers
 */
int add(int a, int b)
{
	return (a + b);
}
